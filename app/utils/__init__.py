"""
app/utils/__init__.py — Utilities Package
============================================
Why this file exists:
    Makes `utils/` a Python package. Contains cross-cutting helpers
    (decorators, validators, formatters) used by multiple routes
    and services — prevents code duplication.
"""
